#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/text_adventures"

if ARGV.empty? || ARGV.first == "server"
  TextAdventures::Web::Server.from_env.start
else
  warn "Usage: bin/text_adventures [server]"
  exit 64
end
